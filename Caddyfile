:4000 {
        bind localhost
        route /api/* {
                uri strip_prefix /api/
                rewrite * /{uri} # rewrite the request without the "/api" prefix
                reverse_proxy localhost:8080
        }
        route {
                reverse_proxy localhost:3000
        }
}
